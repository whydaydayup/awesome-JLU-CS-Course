#include <iostream>
#include "Stack.h"
using namespace std;

int main()
{
    /*
    cout << "please input the max size of the stack:" << endl;
    int n(0);
    cin >> n;
    Stack<int> astack(n);
    cout << "please input the actual length of the stack:" << endl;
    cin >> n;
    for(int i=0;i<n;++i)
    {
        astack.create(i,i);
    }
    astack.print();
    astack.pop();
    astack.print();
    astack.push(6);
    astack.print();
    astack.peek(n);
    cout << n;*/
    return 0;
}
